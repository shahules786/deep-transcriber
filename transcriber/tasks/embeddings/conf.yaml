data:
  train: './data/train'
  test:  './data/test'
  logger : 'INFO'

model:
  input_size: 40
  hidden_size: 768
  num_layers: 3
  embedding_dim: 256
  model_dir: './model/'

training:
  n_speakers: 64
  n_utterances: 10
  batch_size: 64
  lr: 0.01
  epochs: 50
  experiment_name: 'deep-transcriber'
  run_name: 'baseline'

